export type Monumento = {
  nombre: string;
  descripcion: string;
  zip: number;
  ciudad: string;
  pais: string;
  continente: string;
  hora: string;
  clima: string;
};

export type Place = {
  ciudad: string;
  pais: string;
  continente: string;
};
